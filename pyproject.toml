[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nextx-workbench"
version = "0.3.0"
description = "Local-first X growth decision workbench"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
nextx = "nextx.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
